package com.atf.test.runner;

import io.cucumber.junit.platform.engine.Cucumber;

@Cucumber
public class TestRunner {
    // No code needed; Cucumber uses annotations and discovers features automatically
}
